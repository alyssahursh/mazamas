<%= form_for(@climb_app) do |f| %>
  <% if @climb_app.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@climb_app.errors.count, "error") %> prohibited this climb_app from being saved:</h2>

      <ul>
      <% @climb_app.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
