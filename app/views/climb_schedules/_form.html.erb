<%= form_for(@climb_schedule) do |f| %>
  <% if @climb_schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@climb_schedule.errors.count, "error") %> prohibited this climb_schedule from being saved:</h2>

      <ul>
      <% @climb_schedule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
